/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i2.wp.com'], // Thay 'example.com' bằng domain của hình ảnh bạn muốn sử dụng
      },
};

export default nextConfig;
